---
- name: Install Git
  hosts: all
  become: yes

  tasks:
    - name: Update apt package cache
      apt:
        update_cache: yes
      become: yes

    - name: Install Git
      apt:
        name: git
        state: present
      become: yes
