---
- name: Run docker-compose up -d from Git
  hosts: all
  become: true

  tasks:
    - name: Change directory to /docker
      shell: cd /docker
      args:
        chdir: /home/johfanang/docker

    - name: Run docker-compose up -d
      shell: docker-compose up -d
      args:
        chdir: /home/johfanang/docker
